## Remote AI deploy assets

Files in this folder:
- `docker-compose.ollama.yml` -> run a remote Ollama service
- `nginx-ollama.conf.example` -> protect the AI endpoint behind Nginx

Use these together with `server/REMOTE_AI_DEPLOYMENT.md`.
